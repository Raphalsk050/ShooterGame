using UnityEngine;

namespace Core.StateMachine.Player.EquipmentState
{
    public class RifleState : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
